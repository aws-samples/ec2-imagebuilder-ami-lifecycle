name: InstallGenericComponent
description: this document installs a generic component
schemaVersion: 1.0

phases:
  - name: build
    steps:
      - name: InstallGenericComponent
        action: ExecuteBash
        inputs:
          commands:
          - sudo yum -y update
          - sudo yum -y install jq